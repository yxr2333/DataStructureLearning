
#include <bits/stdc++.h>
using namespace std;
class InsertionSort
{
public:
    void direct_insert_sort(vector<int> data, int size);
    void binary_insert_sort(vector<int> data, int size);
    void shell_sort(vector<int> data, int size);
    void print(vector<int> &data, int size);
};
